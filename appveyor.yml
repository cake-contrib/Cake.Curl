version: 1.0.0-build.{build}
environment:
  Configuration: Release
  PackageOutputDirectory: Packages
test: off
build_script:
- ps: .\build.ps1 -Configuration $env:Configuration -PackageOutputDirectory $env:PackageOutputDirectory
